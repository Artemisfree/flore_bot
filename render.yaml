services:
  - type: web
    name: flore-api
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVarGroups:
      - flore-common

  - type: worker
    name: flore-bot
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVarGroups:
      - flore-common
